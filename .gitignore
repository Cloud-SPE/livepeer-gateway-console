# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Build output
dist/
build/
*.tsbuildinfo

# Test coverage
coverage/
.nyc_output/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store

# Local config / secrets
.env
.env.local
.env.*.local
*.key
*.pem
keystore.json
keystore-password

# Data / local state
data/
*.db
*.sqlite

# Logs
*.log
logs/

# buf-generated stubs are now committed (Plan 0001 §post-MVP — the
# tracker entry "bootstrap-from-clean-clone" item closes by inverting
# this rule). Re-run `npm run proto:gen` locally when an upstream
# daemon proto changes; CI's `proto:check` enforces no drift.
#
# Note: do NOT add src/providers/payerDaemon/gen/ or
# src/providers/resolver/gen/ here.
