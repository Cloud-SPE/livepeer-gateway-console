// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.6
//   protoc               unknown
// source: livepeer/registry/v1/types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "livepeer.registry.v1";

/** ResolveMode mirrors types.ResolveMode in the Go layer. Frozen v1. */
export enum ResolveMode {
  RESOLVE_MODE_UNSPECIFIED = 0,
  RESOLVE_MODE_WELL_KNOWN = 1,
  RESOLVE_MODE_CSV = 2,
  RESOLVE_MODE_LEGACY = 3,
  UNRECOGNIZED = -1,
}

export function resolveModeFromJSON(object: any): ResolveMode {
  switch (object) {
    case 0:
    case "RESOLVE_MODE_UNSPECIFIED":
      return ResolveMode.RESOLVE_MODE_UNSPECIFIED;
    case 1:
    case "RESOLVE_MODE_WELL_KNOWN":
      return ResolveMode.RESOLVE_MODE_WELL_KNOWN;
    case 2:
    case "RESOLVE_MODE_CSV":
      return ResolveMode.RESOLVE_MODE_CSV;
    case 3:
    case "RESOLVE_MODE_LEGACY":
      return ResolveMode.RESOLVE_MODE_LEGACY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ResolveMode.UNRECOGNIZED;
  }
}

export function resolveModeToJSON(object: ResolveMode): string {
  switch (object) {
    case ResolveMode.RESOLVE_MODE_UNSPECIFIED:
      return "RESOLVE_MODE_UNSPECIFIED";
    case ResolveMode.RESOLVE_MODE_WELL_KNOWN:
      return "RESOLVE_MODE_WELL_KNOWN";
    case ResolveMode.RESOLVE_MODE_CSV:
      return "RESOLVE_MODE_CSV";
    case ResolveMode.RESOLVE_MODE_LEGACY:
      return "RESOLVE_MODE_LEGACY";
    case ResolveMode.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** SignatureStatus mirrors types.SignatureStatus. */
export enum SignatureStatus {
  SIGNATURE_STATUS_UNSPECIFIED = 0,
  SIGNATURE_STATUS_VERIFIED = 1,
  SIGNATURE_STATUS_UNSIGNED = 2,
  SIGNATURE_STATUS_LEGACY = 3,
  UNRECOGNIZED = -1,
}

export function signatureStatusFromJSON(object: any): SignatureStatus {
  switch (object) {
    case 0:
    case "SIGNATURE_STATUS_UNSPECIFIED":
      return SignatureStatus.SIGNATURE_STATUS_UNSPECIFIED;
    case 1:
    case "SIGNATURE_STATUS_VERIFIED":
      return SignatureStatus.SIGNATURE_STATUS_VERIFIED;
    case 2:
    case "SIGNATURE_STATUS_UNSIGNED":
      return SignatureStatus.SIGNATURE_STATUS_UNSIGNED;
    case 3:
    case "SIGNATURE_STATUS_LEGACY":
      return SignatureStatus.SIGNATURE_STATUS_LEGACY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SignatureStatus.UNRECOGNIZED;
  }
}

export function signatureStatusToJSON(object: SignatureStatus): string {
  switch (object) {
    case SignatureStatus.SIGNATURE_STATUS_UNSPECIFIED:
      return "SIGNATURE_STATUS_UNSPECIFIED";
    case SignatureStatus.SIGNATURE_STATUS_VERIFIED:
      return "SIGNATURE_STATUS_VERIFIED";
    case SignatureStatus.SIGNATURE_STATUS_UNSIGNED:
      return "SIGNATURE_STATUS_UNSIGNED";
    case SignatureStatus.SIGNATURE_STATUS_LEGACY:
      return "SIGNATURE_STATUS_LEGACY";
    case SignatureStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** FreshnessStatus mirrors types.FreshnessStatus. */
export enum FreshnessStatus {
  FRESHNESS_STATUS_UNSPECIFIED = 0,
  FRESHNESS_STATUS_FRESH = 1,
  FRESHNESS_STATUS_STALE_RECOVERABLE = 2,
  FRESHNESS_STATUS_STALE_FAILING = 3,
  UNRECOGNIZED = -1,
}

export function freshnessStatusFromJSON(object: any): FreshnessStatus {
  switch (object) {
    case 0:
    case "FRESHNESS_STATUS_UNSPECIFIED":
      return FreshnessStatus.FRESHNESS_STATUS_UNSPECIFIED;
    case 1:
    case "FRESHNESS_STATUS_FRESH":
      return FreshnessStatus.FRESHNESS_STATUS_FRESH;
    case 2:
    case "FRESHNESS_STATUS_STALE_RECOVERABLE":
      return FreshnessStatus.FRESHNESS_STATUS_STALE_RECOVERABLE;
    case 3:
    case "FRESHNESS_STATUS_STALE_FAILING":
      return FreshnessStatus.FRESHNESS_STATUS_STALE_FAILING;
    case -1:
    case "UNRECOGNIZED":
    default:
      return FreshnessStatus.UNRECOGNIZED;
  }
}

export function freshnessStatusToJSON(object: FreshnessStatus): string {
  switch (object) {
    case FreshnessStatus.FRESHNESS_STATUS_UNSPECIFIED:
      return "FRESHNESS_STATUS_UNSPECIFIED";
    case FreshnessStatus.FRESHNESS_STATUS_FRESH:
      return "FRESHNESS_STATUS_FRESH";
    case FreshnessStatus.FRESHNESS_STATUS_STALE_RECOVERABLE:
      return "FRESHNESS_STATUS_STALE_RECOVERABLE";
    case FreshnessStatus.FRESHNESS_STATUS_STALE_FAILING:
      return "FRESHNESS_STATUS_STALE_FAILING";
    case FreshnessStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Source mirrors types.Source. */
export enum Source {
  SOURCE_UNSPECIFIED = 0,
  SOURCE_MANIFEST = 1,
  SOURCE_LEGACY = 2,
  SOURCE_STATIC_OVERLAY = 3,
  SOURCE_CSV_FALLBACK = 4,
  UNRECOGNIZED = -1,
}

export function sourceFromJSON(object: any): Source {
  switch (object) {
    case 0:
    case "SOURCE_UNSPECIFIED":
      return Source.SOURCE_UNSPECIFIED;
    case 1:
    case "SOURCE_MANIFEST":
      return Source.SOURCE_MANIFEST;
    case 2:
    case "SOURCE_LEGACY":
      return Source.SOURCE_LEGACY;
    case 3:
    case "SOURCE_STATIC_OVERLAY":
      return Source.SOURCE_STATIC_OVERLAY;
    case 4:
    case "SOURCE_CSV_FALLBACK":
      return Source.SOURCE_CSV_FALLBACK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Source.UNRECOGNIZED;
  }
}

export function sourceToJSON(object: Source): string {
  switch (object) {
    case Source.SOURCE_UNSPECIFIED:
      return "SOURCE_UNSPECIFIED";
    case Source.SOURCE_MANIFEST:
      return "SOURCE_MANIFEST";
    case Source.SOURCE_LEGACY:
      return "SOURCE_LEGACY";
    case Source.SOURCE_STATIC_OVERLAY:
      return "SOURCE_STATIC_OVERLAY";
    case Source.SOURCE_CSV_FALLBACK:
      return "SOURCE_CSV_FALLBACK";
    case Source.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Capability is one named operation a node advertises. The name is
 * opaque to the registry; consumers interpret it.
 */
export interface Capability {
  name: string;
  workUnit: string;
  offerings: Offering[];
  /** arbitrary JSON, opaque */
  extraJson: Buffer;
}

/**
 * Offering is a priced tier under a capability. The id is opaque to the
 * registry — for AI workloads it's typically the model name (e.g.
 * "gpt-oss-20b"); for video transcoding it's a preset id (e.g.
 * "h264-1080p"); for streaming sessions a resolution/fps tier (e.g.
 * "vtuber-1080p30"). Pricing is the per-work-unit wholesale rate the
 * orchestrator advertises; consumers (gateways/bridges) read it as the
 * wholesale-side input to their routing decision.
 */
export interface Offering {
  id: string;
  /** decimal big-int as string */
  pricePerWorkUnitWei: string;
  /** arbitrary JSON object, opaque */
  constraintsJson: Buffer;
}

/** Node is one orchestrator endpoint with resolver-attached metadata. */
export interface Node {
  id: string;
  url: string;
  workerEthAddress: string;
  /** arbitrary JSON object, opaque */
  extraJson: Buffer;
  capabilities: Capability[];
  source: Source;
  signatureStatus: SignatureStatus;
  /** 0x-prefixed lower-case */
  operatorAddress: string;
  enabled: boolean;
  tierAllowed: string[];
  weight: number;
}

function createBaseCapability(): Capability {
  return { name: "", workUnit: "", offerings: [], extraJson: Buffer.alloc(0) };
}

export const Capability: MessageFns<Capability> = {
  encode(message: Capability, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.workUnit !== "") {
      writer.uint32(18).string(message.workUnit);
    }
    for (const v of message.offerings) {
      Offering.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.extraJson.length !== 0) {
      writer.uint32(34).bytes(message.extraJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Capability {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCapability();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.workUnit = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.offerings.push(Offering.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.extraJson = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Capability {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      workUnit: isSet(object.workUnit)
        ? globalThis.String(object.workUnit)
        : isSet(object.work_unit)
        ? globalThis.String(object.work_unit)
        : "",
      offerings: globalThis.Array.isArray(object?.offerings)
        ? object.offerings.map((e: any) => Offering.fromJSON(e))
        : [],
      extraJson: isSet(object.extraJson)
        ? Buffer.from(bytesFromBase64(object.extraJson))
        : isSet(object.extra_json)
        ? Buffer.from(bytesFromBase64(object.extra_json))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: Capability): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.workUnit !== "") {
      obj.workUnit = message.workUnit;
    }
    if (message.offerings?.length) {
      obj.offerings = message.offerings.map((e) => Offering.toJSON(e));
    }
    if (message.extraJson.length !== 0) {
      obj.extraJson = base64FromBytes(message.extraJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Capability>, I>>(base?: I): Capability {
    return Capability.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Capability>, I>>(object: I): Capability {
    const message = createBaseCapability();
    message.name = object.name ?? "";
    message.workUnit = object.workUnit ?? "";
    message.offerings = object.offerings?.map((e) => Offering.fromPartial(e)) || [];
    message.extraJson = object.extraJson ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseOffering(): Offering {
  return { id: "", pricePerWorkUnitWei: "", constraintsJson: Buffer.alloc(0) };
}

export const Offering: MessageFns<Offering> = {
  encode(message: Offering, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.pricePerWorkUnitWei !== "") {
      writer.uint32(18).string(message.pricePerWorkUnitWei);
    }
    if (message.constraintsJson.length !== 0) {
      writer.uint32(26).bytes(message.constraintsJson);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Offering {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOffering();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.pricePerWorkUnitWei = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.constraintsJson = Buffer.from(reader.bytes());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Offering {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      pricePerWorkUnitWei: isSet(object.pricePerWorkUnitWei)
        ? globalThis.String(object.pricePerWorkUnitWei)
        : isSet(object.price_per_work_unit_wei)
        ? globalThis.String(object.price_per_work_unit_wei)
        : "",
      constraintsJson: isSet(object.constraintsJson)
        ? Buffer.from(bytesFromBase64(object.constraintsJson))
        : isSet(object.constraints_json)
        ? Buffer.from(bytesFromBase64(object.constraints_json))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: Offering): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.pricePerWorkUnitWei !== "") {
      obj.pricePerWorkUnitWei = message.pricePerWorkUnitWei;
    }
    if (message.constraintsJson.length !== 0) {
      obj.constraintsJson = base64FromBytes(message.constraintsJson);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Offering>, I>>(base?: I): Offering {
    return Offering.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Offering>, I>>(object: I): Offering {
    const message = createBaseOffering();
    message.id = object.id ?? "";
    message.pricePerWorkUnitWei = object.pricePerWorkUnitWei ?? "";
    message.constraintsJson = object.constraintsJson ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseNode(): Node {
  return {
    id: "",
    url: "",
    workerEthAddress: "",
    extraJson: Buffer.alloc(0),
    capabilities: [],
    source: 0,
    signatureStatus: 0,
    operatorAddress: "",
    enabled: false,
    tierAllowed: [],
    weight: 0,
  };
}

export const Node: MessageFns<Node> = {
  encode(message: Node, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.url !== "") {
      writer.uint32(18).string(message.url);
    }
    if (message.workerEthAddress !== "") {
      writer.uint32(26).string(message.workerEthAddress);
    }
    if (message.extraJson.length !== 0) {
      writer.uint32(34).bytes(message.extraJson);
    }
    for (const v of message.capabilities) {
      Capability.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.source !== 0) {
      writer.uint32(48).int32(message.source);
    }
    if (message.signatureStatus !== 0) {
      writer.uint32(56).int32(message.signatureStatus);
    }
    if (message.operatorAddress !== "") {
      writer.uint32(66).string(message.operatorAddress);
    }
    if (message.enabled !== false) {
      writer.uint32(72).bool(message.enabled);
    }
    for (const v of message.tierAllowed) {
      writer.uint32(82).string(v!);
    }
    if (message.weight !== 0) {
      writer.uint32(88).int32(message.weight);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Node {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.url = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.workerEthAddress = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.extraJson = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.capabilities.push(Capability.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.source = reader.int32() as any;
          continue;
        }
        case 7: {
          if (tag !== 56) {
            break;
          }

          message.signatureStatus = reader.int32() as any;
          continue;
        }
        case 8: {
          if (tag !== 66) {
            break;
          }

          message.operatorAddress = reader.string();
          continue;
        }
        case 9: {
          if (tag !== 72) {
            break;
          }

          message.enabled = reader.bool();
          continue;
        }
        case 10: {
          if (tag !== 82) {
            break;
          }

          message.tierAllowed.push(reader.string());
          continue;
        }
        case 11: {
          if (tag !== 88) {
            break;
          }

          message.weight = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Node {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      workerEthAddress: isSet(object.workerEthAddress)
        ? globalThis.String(object.workerEthAddress)
        : isSet(object.worker_eth_address)
        ? globalThis.String(object.worker_eth_address)
        : "",
      extraJson: isSet(object.extraJson)
        ? Buffer.from(bytesFromBase64(object.extraJson))
        : isSet(object.extra_json)
        ? Buffer.from(bytesFromBase64(object.extra_json))
        : Buffer.alloc(0),
      capabilities: globalThis.Array.isArray(object?.capabilities)
        ? object.capabilities.map((e: any) => Capability.fromJSON(e))
        : [],
      source: isSet(object.source) ? sourceFromJSON(object.source) : 0,
      signatureStatus: isSet(object.signatureStatus)
        ? signatureStatusFromJSON(object.signatureStatus)
        : isSet(object.signature_status)
        ? signatureStatusFromJSON(object.signature_status)
        : 0,
      operatorAddress: isSet(object.operatorAddress)
        ? globalThis.String(object.operatorAddress)
        : isSet(object.operator_address)
        ? globalThis.String(object.operator_address)
        : "",
      enabled: isSet(object.enabled) ? globalThis.Boolean(object.enabled) : false,
      tierAllowed: globalThis.Array.isArray(object?.tierAllowed)
        ? object.tierAllowed.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.tier_allowed)
        ? object.tier_allowed.map((e: any) => globalThis.String(e))
        : [],
      weight: isSet(object.weight) ? globalThis.Number(object.weight) : 0,
    };
  },

  toJSON(message: Node): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.workerEthAddress !== "") {
      obj.workerEthAddress = message.workerEthAddress;
    }
    if (message.extraJson.length !== 0) {
      obj.extraJson = base64FromBytes(message.extraJson);
    }
    if (message.capabilities?.length) {
      obj.capabilities = message.capabilities.map((e) => Capability.toJSON(e));
    }
    if (message.source !== 0) {
      obj.source = sourceToJSON(message.source);
    }
    if (message.signatureStatus !== 0) {
      obj.signatureStatus = signatureStatusToJSON(message.signatureStatus);
    }
    if (message.operatorAddress !== "") {
      obj.operatorAddress = message.operatorAddress;
    }
    if (message.enabled !== false) {
      obj.enabled = message.enabled;
    }
    if (message.tierAllowed?.length) {
      obj.tierAllowed = message.tierAllowed;
    }
    if (message.weight !== 0) {
      obj.weight = Math.round(message.weight);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Node>, I>>(base?: I): Node {
    return Node.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Node>, I>>(object: I): Node {
    const message = createBaseNode();
    message.id = object.id ?? "";
    message.url = object.url ?? "";
    message.workerEthAddress = object.workerEthAddress ?? "";
    message.extraJson = object.extraJson ?? Buffer.alloc(0);
    message.capabilities = object.capabilities?.map((e) => Capability.fromPartial(e)) || [];
    message.source = object.source ?? 0;
    message.signatureStatus = object.signatureStatus ?? 0;
    message.operatorAddress = object.operatorAddress ?? "";
    message.enabled = object.enabled ?? false;
    message.tierAllowed = object.tierAllowed?.map((e) => e) || [];
    message.weight = object.weight ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
